{
  "system": {
    "name": "Pipes and Filters System",
    "description": "A system that processes a stream of data in stages using the Pipes and Filters pattern.",
    "components": {
      "pipeline": {
        "name": "Pipes and Filters",
        "description": "A component that processes a stream of data in stages, where each stage (filter) performs a specific transformation.",
        "parameters": {
            "N_A": { "type": "integer", "description": "Number of users for data path A." },
            "N_B": { "type": "integer", "description": "Number of users for data path B." }
        },
        "decisions": {
          "pipeline_structure": {
            "description": "The structure of the pipeline, either with joint or separated filters.",
            "policies": {
              "separated": { "description": "Filters for different data paths are separate." },
              "joint1": { "description": "Filters are shared between data paths (config 1)." },
              "joint2": { "description": "Filters are shared between data paths (config 2)." }
            }
          }
        }
      }
    }
  },
  "dataspace": {
    "policy_identification": {
      "from": "file",
      "policies": [
        {
          "name": "separated-system",
          "description": "System with separated pipelines.",
          "source_file": "separated/dataset.csv",
          "component_policies": { "pipeline": "separated" }
        },
        {
          "name": "joint1-system",
          "description": "System with a joint pipeline (config 1).",
          "source_file": "joint/dataset_1.csv",
          "component_policies": { "pipeline": "joint1" }
        },
        {
          "name": "joint2-system",
          "description": "System with a joint pipeline (config 2).",
          "source_file": "joint/dataset_2.csv",
          "component_policies": { "pipeline": "joint2" }
        }
      ]
    },
    "column_renames": { "R0": "response_time", "X0": "throughput" },
    "quality_objectives": [
      {
        "name": "response_time",
        "metric": "ms",
        "description": "Average response time for data processing.",
        "maximize": false
      },
      {
        "name": "throughput",
        "metric": "ops/sec",
        "description": "Overall throughput of the pipeline.",
        "maximize": true
      }
    ]
  },
  "metadata": {
    "version": "2.0",
    "notes": "Parameter definitions are truncated for brevity."
  }
}
