{
  "system": {
    "name": "BFF System",
    "description": "A system implementing the Backend for Frontends pattern.",
    "components": {
      "bff_component": {
        "name": "Backend for Frontends",
        "description": "A pattern that creates separate backend services for different frontend experiences, such as mobile and web.",
        "parameters": {},
        "decisions": {
          "bff_implementation": {
            "description": "The specific implementation of the BFF.",
            "policies": {
              "multi-bff": { "description": "A policy with multiple BFFs for different client types." }
            }
          }
        }
      }
    }
  },
  "dataspace": {
    "policy_identification": {
      "from": "file",
      "policies": [
        {
          "name": "multi-bff-system",
          "description": "System implementing multiple BFFs.",
          "source_file": "multi_N100.csv",
          "component_policies": {
            "bff_component": "multi-bff"
          }
        }
      ]
    },
    "quality_objectives": [
      {
        "name": "response_time",
        "metric": "ms",
        "description": "The end-to-end time to serve a request.",
        "maximize": false
      },
      {
        "name": "utilization",
        "metric": "percentage",
        "description": "The utilization of the backend resource.",
        "maximize": false
      }
    ]
  },
  "metadata": {
    "version": "2.0",
    "notes": "BFF pattern data is minimal. The loader in archspace.py is empty, so this definition is based on the available data file 'multi_N100.csv' and is largely a placeholder. Quality objectives are inferred and may need correction."
  }
}
