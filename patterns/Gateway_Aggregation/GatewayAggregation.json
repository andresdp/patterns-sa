{
  "system": {
    "name": "Gateway Aggregation System",
    "description": "A system implementing the Gateway Aggregation pattern.",
    "components": {
      "aggregator_gateway": {
        "name": "Gateway Aggregation",
        "description": "A pattern where a gateway aggregates requests to multiple microservices into a single client request.",
        "parameters": {
          "N_A": { "type": "integer", "description": "Number of users of type A." },
          "N_B": { "type": "integer", "description": "Number of users of type B." },
          "r_Z_A": { "type": "float", "description": "Rate of think time for users of type A." },
          "r_Z_B": { "type": "float", "description": "Rate of think time for users of type B." }
        },
        "decisions": {
          "aggregation_scope": {
            "description": "The scope of services being aggregated by the gateway.",
            "policies": {
              "default-aggregation": { "description": "A default aggregation of 3 services." }
            }
          }
        }
      }
    }
  },
  "dataspace": {
    "source_file": "multi_N25.csv",
    "column_renames": { "R0": "response_time", "Ugw": "utilization" },
    "policy_identification": {
      "from": "column",
      "column": "policy",
      "policies": {
        "0": {
          "name": "default-system",
          "description": "System policy with the default aggregation.",
          "component_policies": {
            "aggregator_gateway": "default-aggregation"
          }
        }
      }
    },
    "quality_objectives": [
      {
        "name": "response_time",
        "metric": "ms",
        "description": "The end-to-end time to serve a request.",
        "maximize": false
      },
      {
        "name": "utilization",
        "metric": "percentage",
        "description": "The utilization of the gateway resource.",
        "maximize": false
      }
    ]
  },
  "metadata": { "version": "2.0" }
}
